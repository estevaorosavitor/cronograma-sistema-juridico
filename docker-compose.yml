services:
  cronograma:
    build: .
    container_name: cronograma-juridico
    restart: unless-stopped
    volumes:
      - ./data.json:/usr/share/nginx/html/data.json:ro
    networks:
      - proxy-net
networks:
  proxy-net:
    external: true